# Skill: GitHub 版本控制與 Pages 部署手冊

本文件載明將專案推送到 GitHub 並自動部署為網頁的標準流程。

## 1. 初始化與同步 GitHub (Git Workflow)
若你尚未建立遠端倉庫，請依序執行：

```powershell
# 1. 檢查 Git 狀態
git status

# 2. 加入所有變更 (包含圖示與 Skill 文件)
git add .

# 3. 提交變更 (請遵守 conventional commits)
git commit -m "feat: upgrade premium visuals and add skill docs"

# 4. 關聯遠端倉庫 (若尚未關聯)
# git remote add origin https://github.com/EVSDELAT/Secret.git

# 5. 推送至主分支
git push -u origin main
```

## 2. 部署至 GitHub Pages (Deployment)
專案已配置 `gh-pages` 工具，執行以下單一指令即可完成打包與發布：

```powershell
npm run deploy
```

### 部署細節：
- **Predeploy**: 自動執行 `npm run build` 生成 `dist` 檔案目錄。
- **Deploy**: 將 `dist` 內容推送至遠端 `gh-pages` 分支。
- **網址**: [https://EVSDELAT.github.io/Secret](https://EVSDELAT.github.io/Secret)

---
*Generated by Antigravity AI*
